#include <iostream>
using namespace std;

int main(){
    float salary[10], total=0, avg;

    for(int i=0;i<10;i++){
        cin>>salary[i];
        total += salary[i];
    }

    avg = total/10;

    cout<<"Total Salary = "<<total<<endl;
    cout<<"Average Salary = "<<avg;
}

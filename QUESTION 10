#include <iostream>
using namespace std;

void callCounter() {
    static int count = 0;   // retains value between calls
    count++;
    cout << "Function called " << count << " times" << endl;
}

int main() {
    callCounter();
    callCounter();
    callCounter();
    callCounter();
    callCounter();

    return 0;
}
